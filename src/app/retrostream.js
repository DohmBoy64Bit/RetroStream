import { installCarousels } from './carousels.js';
const $=s=>document.querySelector(s);
const main=$('#main');
installCarousels(main);
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const star='<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="m12 2 2.9 6.2 6.8.8-5 4.7 1.4 6.7-6.1-3.4-6.1 3.4 1.4-6.7-5-4.7 6.8-.8z"/></svg>';
const play='<svg viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><path d="m7 4 14 8-14 8z"/></svg>';
const info='<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" aria-hidden="true"><circle cx="12" cy="12" r="9"/><path d="M12 11v6M12 7v1"/></svg>';
let imageBase='https://image.tmdb.org/t/p/',genres={movie:[],tv:[]},generation=0,featured=[],featureIndex=0;
const cache=new Map();
async function api(path,params={}){const url='/api/tmdb?'+new URLSearchParams({path,...params});if(cache.has(url))return cache.get(url);const r=await fetch(url);const d=await r.json();if(!r.ok)throw Error(d.error||'Could not load the catalog.');cache.set(url,d);return d;}
const name=m=>m.title||m.name||'Untitled'; const date=m=>(m.release_date||m.first_air_date||'').slice(0,4); const type=m=>m.media_type||(m.title?'movie':'tv');
const img=(path,size='w500')=>path?imageBase+size+path:'';
function photo(path,alt,cls='',size='w500',eager=false){const fallback=cls==='feature-photo'?'feature-photo artwork-fallback':size==='w185'?'person-fallback':size==='w300'?'still-fallback':'poster-fallback';return path?`<img class="${cls}" src="${esc(img(path,size))}" alt="${esc(alt)}" loading="${eager?'eager':'lazy'}" ${eager?'fetchpriority="high"':''} onerror="this.replaceWith(Object.assign(document.createElement('div'),{className:'${fallback}'}))">`:`<div class="${fallback}" role="img" aria-label="Image unavailable"></div>`;}
const score=m=>m.vote_count?`<span class="rating">${star}${Number(m.vote_average).toFixed(1)}</span>`:'<span>Not rated</span>';
function cards(items){return items.filter(m=>type(m)!=='person'&&!m.adult).map(m=>`<a class="poster-card" href="#title/${type(m)}/${m.id}" aria-label="${esc(name(m))}, ${date(m)}, ${type(m)==='tv'?'TV series':'movie'}"><div class="poster-wrap">${photo(m.poster_path,name(m))}<span class="poster-score">${score(m)}</span><span class="poster-type">${type(m)==='tv'?'SERIES':'FILM'}</span></div><h3>${esc(name(m))}</h3><p><span>${date(m)||'Release TBA'}</span><span>${esc((genres[type(m)]||[]).find(g=>m.genre_ids?.includes(g.id))?.name||'')}</span></p></a>`).join('');}
function loading(){return '<div class="loading-screen" role="status"><span class="loading loading-spinner"></span> Loading the collectionâ¦</div>';}
function errorHtml(e){return `<div class="error-box" role="alert"><h2>That didnât load.</h2><p>${esc(e.message)}</p><button class="btn btn-secondary" data-retry>Try again</button></div>`;}
function feature(m,detail=false){return `<section class="feature ${detail?'detail-hero':''}">${photo(m.backdrop_path,'','feature-photo','w1280',true)}<div class="feature-copy"><h1>${esc(name(m))}</h1><div class="meta">${score(m)}<span>${date(m)}</span><span>${m.runtime?Math.floor(m.runtime/60)+'h '+m.runtime%60+'m':type(m)==='tv'?(m.number_of_seasons?m.number_of_seasons+' seasons':'TV series'):'Movie'}</span><span>${esc((m.genres||genres[type(m)]?.filter(g=>m.genre_ids?.includes(g.id))||[]).slice(0,2).map(g=>g.name).join(' Â· '))}</span></div><p>${esc(m.overview||'Explore the cast, details, and available episodes.')}</p><div class="actions"><a class="btn btn-primary" href="${type(m)==='tv'?'#title/tv/'+m.id+'?episodes=1':'#watch/movie/'+m.id}">${play}${type(m)==='tv'?'Choose episode':'Watch movie'}</a>${detail?'':`<a class="btn btn-secondary" href="#title/${type(m)}/${m.id}">${info}More info</a>`}</div></div>${detail?'':`<div class="feature-counter" aria-label="Featured titles" role="group">${featured.map((f,i)=>`<button data-feature="${i}" class="${i===featureIndex?'active':''}" aria-label="Show featured title ${i+1} of ${featured.length}: ${esc(name(f))}" aria-pressed="${i===featureIndex}"></button>`).join('')}</div>`}</section>`;}
function section(title,items,href,extra=''){return `<section class="section"><div class="section-head"><div class="section-left"><h2>${title}</h2>${extra}</div><a href="${href}">View all &rarr;</a></div><div class="shelf">${cards(items.slice(0,12))}</div></section>`;}
async function home(g){const results=await Promise.allSettled([api('trending/all/week'),api('movie/popular'),api('tv/popular'),api('movie/now_playing')]);if(g!==generation)return;const lists=results.map(r=>r.status==='fulfilled'?r.value.results:[]);if(!lists.some(l=>l.length))throw Error('Unable to reach TMDB. Please try again.');featured=lists[0].filter(m=>m.backdrop_path&&m.overview&&m.media_type!=='person').slice(0,4);featureIndex=0;main.innerHTML=(featured.length?`<div id="featured">${feature(featured[0])}</div>`:'')+`<div class="content"><div class="genre-strip" aria-label="Browse by genre">${['Action','Comedy','Science Fiction','Thriller','Animation','Drama','Horror','Adventure'].map(n=>{const x=genres.movie.find(g=>g.name===n);return x?`<a href="#browse/movie?genre=${x.id}">${esc(n)}</a>`:'';}).join('')}</div>${section('Trending this week',lists[0],'#browse/movie',`<div class="toggle" aria-label="Trending category"><button class="active" data-trending="all">All</button><button data-trending="movie">Movies</button><button data-trending="tv">Series</button></div>`)}${section('The popular picks',lists[1],'#browse/movie')}${section('One more episode',lists[2],'#browse/tv')}${section('Fresh from the cinema',lists[3],'#latest')}</div>`;main.querySelectorAll('[data-trending]').forEach(b=>b.onclick=()=>{main.querySelectorAll('[data-trending]').forEach(x=>x.classList.toggle('active',x===b));b.closest('.section').querySelector('.shelf').innerHTML=cards(lists[0].filter(m=>b.dataset.trending==='all'||type(m)===b.dataset.trending).slice(0,12));});}
function filterMarkup(t,p,search=false){const year=new Date().getFullYear();return `<div class="filters"><label class="sr-only" for="media">Media type</label><select id="media" class="select"><option value="movie" ${t==='movie'?'selected':''}>Movies</option><option value="tv" ${t==='tv'?'selected':''}>TV & series</option>${search?`<option value="all" ${t==='all'?'selected':''}>Movies & series</option>`:''}</select><label class="sr-only" for="genre">Genre</label><select id="genre" class="select"><option value="">All genres</option>${[...new Map([...(genres[t]||genres.movie),...(t==='all'?genres.tv:[])].map(g=>[g.id,g])).values()].map(x=>`<option value="${x.id}" ${p.get('genre')==x.id?'selected':''}>${esc(x.name)}</option>`).join('')}</select><label class="sr-only" for="year">Release year</label><select id="year" class="select"><option value="">Any year</option>${Array.from({length:year-1949},(_,i)=>year-i).map(y=>`<option ${p.get('year')==y?'selected':''}>${y}</option>`).join('')}</select><label class="sr-only" for="rating">Minimum TMDB rating</label><select id="rating" class="select"><option value="">Any rating</option>${[9,8,7,6].map(v=>`<option value="${v}" ${p.get('rating')==v?'selected':''}>${v}+ TMDB rating</option>`).join('')}</select>${search?'':`<label class="sr-only" for="sort">Sort by</label><select id="sort" class="select"><option value="popularity.desc">Most popular</option><option value="vote_average.desc" ${p.get('sort')==='vote_average.desc'?'selected':''}>Top rated</option><option value="primary_release_date.desc" ${p.get('sort')==='primary_release_date.desc'?'selected':''}>Latest releases</option></select>`}<button class="btn btn-ghost" id="reset-filters">Reset</button></div>`;}
async function browse(t,p,g,search=false,latest=false){t=t||'movie';let query=p.get('q')||'';main.innerHTML=`<div class="content"><h1 class="browse-title">${search?'Find your next great watch.':latest?'New and recent releases.':t==='tv'?'Your next obsession.':'A whole world of movies.'}</h1>${search?`<form class="search-form" id="search-form"><label class="sr-only" for="query">Search titles or IMDb IDs</label><input class="input" id="query" placeholder="Search a title or IMDb IDâ¦" aria-describedby="search-hint" value="${esc(query)}" autocomplete="off" maxlength="180"><button class="btn btn-primary">Search</button></form><p class="search-hint" id="search-hint">Search by movie, series, or IMDb ID.</p>`:''}${filterMarkup(t,latest&&!p.has('sort')?new URLSearchParams({...Object.fromEntries(p),sort:'primary_release_date.desc'}):p,search)}<p class="result-summary" aria-live="polite" id="summary"></p><div id="results" class="grid-results"></div><div id="results-state"></div><button id="more" class="btn btn-secondary load-more" hidden>Load more</button></div>`;
let page=0,total=1,found=0,busy=false;const update=()=>{const q=new URLSearchParams();if(search)q.set('q',$('#query').value.trim());for(const k of ['genre','year','rating','sort'])if($('#'+k)?.value)q.set(k,$('#'+k).value);location.hash=(search?'search':latest?'latest':'browse/'+$('#media').value)+'?'+q+(search?'&type='+$('#media').value:latest?'&type='+$('#media').value:'');};
for(const id of ['media','genre','year','rating','sort'])if($('#'+id))$('#'+id).onchange=update;
$('#reset-filters').onclick=()=>location.hash=search?'search?q='+encodeURIComponent(query):latest?'latest':'browse/'+t;
if(search){$('#query').focus();$('#query').setSelectionRange(query.length,query.length);$('#search-form').onsubmit=e=>{e.preventDefault();update();};let timer;$('#query').oninput=()=>{clearTimeout(timer);timer=setTimeout(()=>{if($('#query')&&$('#query').value.trim().length>=2)update();},450);};if(!query){$('#summary').textContent='Movies, series, or a specific IMDb ID. Start with something you love.';$('#query').focus();return;}}
async function more(){if(busy||page>=total||g!==generation)return;busy=true;$('#more').disabled=true;$('#results-state').innerHTML=loading();try{let d;page++;if(search&&/^tt\d+$/.test(query)){const x=await api('find/'+query,{external_source:'imdb_id'});d={results:[...x.movie_results.map(m=>({...m,media_type:'movie'})),...x.tv_results.map(m=>({...m,media_type:'tv'}))],total_pages:1};}else if(search)d=await api('search/'+(t==='all'?'multi':t),{query,page});else {const params={page,sort_by:p.get('sort')||(latest?'primary_release_date.desc':'popularity.desc'),'vote_count.gte':latest?'5':'100'};if(t==='tv'&&params.sort_by==='primary_release_date.desc')params.sort_by='first_air_date.desc';params[t==='tv'?'first_air_date.lte':'primary_release_date.lte']=new Date().toISOString().slice(0,10);if(p.get('genre'))params.with_genres=p.get('genre');if(p.get('year'))params[t==='tv'?'first_air_date_year':'primary_release_year']=p.get('year');if(p.get('rating'))params['vote_average.gte']=p.get('rating');d=await api('discover/'+t,params);}
if(g!==generation)return;total=Math.min(d.total_pages||1,500);let items=d.results.map(m=>({...m,media_type:m.media_type||(t==='all'?type(m):t)})).filter(m=>['movie','tv'].includes(type(m))&&!m.adult);if(search)items=items.filter(m=>(!p.get('genre')||m.genre_ids?.includes(Number(p.get('genre'))))&&(!p.get('year')||date(m)===p.get('year'))&&(!p.get('rating')||m.vote_average>=Number(p.get('rating')))&&(t==='all'||type(m)===t));found+=items.length;$('#results').insertAdjacentHTML('beforeend',cards(items));$('#summary').textContent=search?`${found} matching title${found===1?'':'s'} loaded${query?' for â'+query+'â':''}`:`${found} titles Â· ${t==='tv'?'TV & series':'Movies'}`;$('#results-state').innerHTML=found?'':`<div class="empty"><h2>No matching titles ${page<total?'on this page':'found'}.</h2><p>${page<total?'Load more results or widen your filters.':'Try another title or reset your filters.'}</p></div>`;$('#more').hidden=page>=total;}catch(e){if(g===generation){page--;$('#results-state').innerHTML=errorHtml(e);$('#results-state [data-retry]').onclick=more;}}finally{busy=false;if(g===generation)$('#more').disabled=false;}}
$('#more').onclick=more;await more();}
async function title(t,id,p,g){const m=await api(t+'/'+id,{append_to_response:'credits,external_ids'});if(g!==generation)return;m.media_type=t;main.innerHTML=`${feature(m,true)}<div class="content"><a class="back-link" href="#browse/${t}">&larr; Back to ${t==='tv'?'series':'movies'}</a><div class="detail-columns"><div>${t==='tv'?'<section id="episodes"><div class="section-head"><h2>Episodes</h2><label><span class="sr-only">Select season</span><select class="select" id="season"></select></label></div><div id="episode-list"></div></section>':''}<section><div class="section-head"><h2>The people behind it</h2></div><div class="cast">${(m.credits?.cast||[]).slice(0,15).map(c=>`<div class="cast-person">${photo(c.profile_path,c.name,'','w185')}<h3>${esc(c.name)}</h3><p>${esc(c.character)}</p></div>`).join('')||'<p>Cast information is not available.</p>'}</div><div class="section-head"><h2>Crew</h2></div><div class="cast">${(m.credits?.crew||[]).filter(c=>['Director','Screenplay','Writer','Executive Producer','Director of Photography','Original Music Composer'].includes(c.job)).slice(0,14).map(c=>`<div class="cast-person"><h3>${esc(c.name)}</h3><p>${esc(c.job)}</p></div>`).join('')||'<p>Crew information is not available.</p>'}</div></section></div><aside class="facts"><dl><dt>Genres</dt><dd>${esc(m.genres?.map(g=>g.name).join(', ')||'Not listed')}</dd><dt>Original title</dt><dd>${esc(m.original_title||m.original_name)}</dd><dt>Status</dt><dd>${esc(m.status)}</dd><dt>${t==='tv'?'Created by':'Directed by'}</dt><dd>${esc((t==='tv'?m.created_by||[]:m.credits?.crew?.filter(c=>c.job==='Director')||[]).map(c=>c.name).join(', ')||'Not listed')}</dd><dt>TMDB audience rating</dt><dd>${Number(m.vote_average).toFixed(1)} / 10 Â· ${Number(m.vote_count).toLocaleString()} votes</dd><dt>Production</dt><dd>${esc(m.production_companies?.map(c=>c.name).join(', ')||'Not listed')}</dd></dl></aside></div></div>`;
if(t==='tv'){const seasons=(m.seasons||[]).filter(s=>s.episode_count>0);$('#season').innerHTML=seasons.map(s=>`<option value="${s.season_number}">${esc(s.name)} Â· ${s.episode_count} episodes</option>`).join('');const first=seasons.find(s=>s.season_number>0)||seasons[0];if(first){$('#season').value=first.season_number;let ticket=0;const load=async()=>{const current=++ticket;$('#episode-list').innerHTML=loading();try{const season=await api(`tv/${id}/season/${$('#season').value}`);if(g!==generation||current!==ticket)return;$('#episode-list').innerHTML=episodeRows(m,season);}catch(e){if(g===generation&&current===ticket){$('#episode-list').innerHTML=errorHtml(e);$('#episode-list [data-retry]').onclick=load;}}};$('#season').onchange=load;await load();}else $('#episode-list').innerHTML='<p>No episodes are available yet.</p>';if(g===generation&&p.has('episodes'))$('#episodes')?.scrollIntoView();}}
function episodeRows(m,s){return s.episodes.map(e=>{const available=e.air_date&&e.air_date<=new Date().toISOString().slice(0,10);return `<${available?'a':'div'} ${available?`href="#watch/tv/${m.id}/${s.season_number}/${e.episode_number}"`:''} class="episode"><span class="number">${e.episode_number}</span>${photo(e.still_path,e.name,'','w300')}<div><h3>${esc(e.name)}</h3><p>${esc(e.overview||'Episode description not available.')}</p></div><span class="episode-time">${available?(e.runtime?e.runtime+' min':'Watch'):e.air_date?'Airs '+esc(e.air_date):'Coming soon'}</span></${available?'a':'div'}>`;}).join('');}
async function watch(t,id,s=1,e=1,g){const m=await api(t+'/'+id,{append_to_response:'external_ids'});let season;if(t==='tv')season=await api(`tv/${id}/season/${s}`);if(g!==generation)return;const imdb=m.imdb_id||m.external_ids?.imdb_id;const episode=season?.episodes.find(x=>x.episode_number===Number(e));const valid=t==='movie'||(episode?.air_date&&episode.air_date<=new Date().toISOString().slice(0,10));const src=imdb&&/^tt\d+$/.test(imdb)?`https://vidsrc2.ru/embed/${t}/${imdb}${t==='tv'?'/'+Number(s)+'/'+Number(e):''}?autoplay=0`:null;main.innerHTML=`<div class="content"><a class="back-link" href="#title/${t}/${id}">&larr; Back to title details</a><div class="watch-head"><div><h1>${esc(name(m))}</h1>${episode?`<p>Season ${Number(s)} Â· Episode ${Number(e)} â ${esc(episode.name)}</p>`:''}</div>${t==='tv'?`<div class="actions"><a class="btn btn-secondary" href="#title/tv/${id}?episodes=1">All episodes</a>${season.episodes.some(x=>x.episode_number===Number(e)+1&&x.air_date&&x.air_date<=new Date().toISOString().slice(0,10))?`<a class="btn btn-primary" href="#watch/tv/${id}/${s}/${Number(e)+1}">Next episode &rarr;</a>`:''}</div>`:''}</div>${src&&valid?`<iframe class="player" src="${src}" title="Watch ${esc(name(m))}${episode?' â '+esc(episode.name):''}" allow="autoplay; encrypted-media; fullscreen; picture-in-picture" allowfullscreen referrerpolicy="origin"></iframe><p class="player-note">Player not loading? <button class="link" id="reload-player">Reload player</button>. Playback is supplied by VidSrc and may include ads. Availability varies.</p>`:`<div class="error-box"><h2>Playback isnât available for this title yet.</h2><p>${!valid?'This episode has not aired or could not be found.':'No IMDb identifier is available for this title.'}</p><a class="btn btn-secondary" href="#title/${t}/${id}">Back to details</a></div>`}<p>${esc(episode?.overview||m.overview)}</p></div>`;if($('#reload-player'))$('#reload-player').onclick=()=>{const f=$('.player');f.src=src;};}
async function route(){const g=++generation;const [raw,qs='']=(location.hash.slice(1)||'home').split('?');const parts=raw.split('/'),p=new URLSearchParams(qs);document.querySelectorAll('[data-nav]').forEach(a=>a.classList.toggle('active',a.dataset.nav===(parts[0]==='browse'?parts[1]:parts[0])));main.innerHTML=loading();window.scrollTo(0,0);try{if(parts[0]==='home')await home(g);else if(parts[0]==='search')await browse(p.get('type')||'all',p,g,true);else if(parts[0]==='latest')await browse(p.get('type')||'movie',p,g,false,true);else if(parts[0]==='browse'&&['movie','tv'].includes(parts[1]))await browse(parts[1],p,g);else if(['title','watch'].includes(parts[0])&&['movie','tv'].includes(parts[1])&&/^\d+$/.test(parts[2])){if(parts[0]==='title')await title(parts[1],parts[2],p,g);else await watch(parts[1],parts[2],parts[3]||1,parts[4]||1,g);}else location.hash='home';}catch(e){if(g===generation){main.innerHTML=`<div class="content">${errorHtml(e)}</div>`;main.querySelector('[data-retry]').onclick=route;}}}
main.addEventListener('click',e=>{const b=e.target.closest('[data-feature]');if(b){featureIndex=Number(b.dataset.feature);$('#featured').innerHTML=feature(featured[featureIndex]);}});
$('#search-launch').onclick=()=>{if(location.hash.startsWith('#search'))$('#query')?.focus();else location.hash='search';};document.addEventListener('keydown',e=>{if(e.key==='/'&&!['INPUT','TEXTAREA','SELECT'].includes(document.activeElement.tagName)){e.preventDefault();$('#search-launch').click();}});window.addEventListener('hashchange',route);
async function boot(){await Promise.allSettled([api('configuration').then(c=>imageBase=c.images.secure_base_url),...['movie','tv'].map(t=>api('genre/'+t+'/list').then(d=>genres[t]=d.genres))]);route();}boot();
if(document.modelContext?.registerTool){const lifecycle=new AbortController();window.addEventListener('pagehide',()=>lifecycle.abort(),{once:true});try{Promise.resolve(document.modelContext.registerTool({name:'search_retrostream_catalog',description:'Search the movie and series catalog and show the matching results.',inputSchema:{type:'object',properties:{query:{type:'string',minLength:1,maxLength:180}},required:['query'],additionalProperties:false},annotations:{readOnlyHint:false,untrustedContentHint:true},async execute(input){if(!input||typeof input.query!=='string'||!input.query.trim()||input.query.length>180)throw Error('Enter a title or IMDb ID.');const hash='#search?q='+encodeURIComponent(input.query.trim());history.pushState(null,'',hash);await route();return {query:input.query.trim(),summary:$('#summary')?.textContent||'Catalog could not be loaded.'};}},{signal:lifecycle.signal})).catch(()=>{});}catch{}}
